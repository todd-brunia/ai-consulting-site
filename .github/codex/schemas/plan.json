{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["classification", "markdown"],
  "properties": {
    "classification": {
      "enum": ["focused", "needs-decision", "split-required"]
    },
    "markdown": {
      "type": "string",
      "minLength": 40,
      "maxLength": 12000
    },
    "blockingDecision": {
      "type": "string",
      "minLength": 10,
      "maxLength": 2000
    },
    "splitReason": {
      "type": "string",
      "minLength": 10,
      "maxLength": 2000
    },
    "children": {
      "type": "array",
      "minItems": 2,
      "maxItems": 10,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "title",
          "outcome",
          "acceptanceCriteria",
          "dependencies",
          "includedScope",
          "excludedScope",
          "suggestedLabels"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "minLength": 3,
            "maxLength": 64
          },
          "title": { "type": "string", "minLength": 5, "maxLength": 160 },
          "outcome": { "type": "string", "minLength": 10, "maxLength": 2000 },
          "acceptanceCriteria": {
            "type": "array",
            "minItems": 1,
            "maxItems": 12,
            "items": { "type": "string", "minLength": 3, "maxLength": 500 }
          },
          "dependencies": {
            "type": "array",
            "minItems": 1,
            "maxItems": 12,
            "items": { "type": "string", "minLength": 1, "maxLength": 500 }
          },
          "includedScope": {
            "type": "array",
            "minItems": 1,
            "maxItems": 12,
            "items": { "type": "string", "minLength": 3, "maxLength": 500 }
          },
          "excludedScope": {
            "type": "array",
            "minItems": 1,
            "maxItems": 12,
            "items": { "type": "string", "minLength": 3, "maxLength": 500 }
          },
          "suggestedLabels": {
            "type": "array",
            "maxItems": 10,
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1, "maxLength": 50 }
          }
        }
      }
    }
  },
  "oneOf": [
    {
      "properties": { "classification": { "const": "focused" } },
      "not": { "anyOf": [{ "required": ["blockingDecision"] }, { "required": ["splitReason"] }, { "required": ["children"] }] }
    },
    {
      "properties": { "classification": { "const": "needs-decision" } },
      "required": ["blockingDecision"],
      "not": { "anyOf": [{ "required": ["splitReason"] }, { "required": ["children"] }] }
    },
    {
      "properties": { "classification": { "const": "split-required" } },
      "required": ["splitReason", "children"],
      "not": { "required": ["blockingDecision"] }
    }
  ]
}
